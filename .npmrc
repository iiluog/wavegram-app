# react-scripts@5.0.1 declares an outdated peer dependency on TypeScript
# (^3 || ^4) while this project uses TypeScript 5. This is a known
# Create React App limitation, so we relax peer-dependency resolution.
legacy-peer-deps=true
