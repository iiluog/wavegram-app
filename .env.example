# Base URL of the Wavegram backend API (without trailing slash).
# The app calls "<REACT_APP_API_URL>/api/...".
#
# For local development against a backend running on your machine:
# REACT_APP_API_URL=http://localhost/wavegram
#
# For the hosted backend:
REACT_APP_API_URL=https://bigwave.bigrock.it
